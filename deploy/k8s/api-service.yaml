apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: ai-assisted-apply
    component: api
spec:
  type: ClusterIP
  selector:
    app: ai-assisted-apply
    component: api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
